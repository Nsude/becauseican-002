import ScrollText from "./ScrollText";


const BigTexts = () => {
  return (
    <section className="relative mb-[6.25rem] w-full h-full min-h-[120vh] flex flex-col justify-center items-center">
      <ScrollText text="eye" />
      <ScrollText text="cre-eight" />
      <ScrollText text="bee" />
      <ScrollText text="cause" />
      <ScrollText text="eye-can" />
    </section>
  )
}

export default BigTexts;